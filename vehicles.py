class Vehicle:
        def __init__(self, make, model, year, mileage, adURL, transmission, 
                     fuelType, engineCC, options, details):
                self.make = make
                self.model = model
                self.year = year
                self.mileage = mileage
                self.adURL = adURL
                self.transmission = transmission
                self.fuel = fuelType
                self.engineCapacity = engineCC
                self.options = options
                self.details = details
                